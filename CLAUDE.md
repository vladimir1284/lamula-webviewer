# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Estado del repo

**F1 (contrato + DAL) implementado** sobre el esqueleto de F0. Piezas: tipos+Zod del contrato en `shared/contract/` (compartido server/cliente), DAL en `server/dal/` (adaptador live D1 y adaptador fixture con grabaciones en `server/dal/fixtures/`, switch `NUXT_DAL_ADAPTER=fixture`), server routes completas (`radars`, `products`, `rasters/{times,closest,next,prev}`, `phenomena[/series]`, `vwp`, `health`), contract tests en `tests/contract/` (snapshot del SQL del pipeline + better-sqlite3; drift via `scripts/check-contract-drift.sh` en CI), suite dual en `tests/unit/dal.spec.ts` (misma suite contra ambos adaptadores + paridad — puerta M1), COGs golden reales en `tests/fixtures/cogs/`, e2e Playwright en modo fixture sobre workerd. Fixtures **grabadas de la D1 real** (`scripts/record-fixtures.sh`; credenciales en `.env`, gitignorado); expectativas de tests derivadas de las grabaciones (`tests/helpers/derive.ts`) — re-grabar no rompe tests. Adaptador live validado contra la D1 real vía preview deployment de Pages. R2 público listo: `r2BaseUrl` apunta a `https://nexrad-raster.ladetec.com` (default en `nuxt.config.ts`, override vía `NUXT_PUBLIC_R2_BASE_URL`) y el bucket tiene CORS (GET/HEAD + `Range`; pages.dev, previews y **solo `localhost:3000`** en local — verificado, no cubre el 8788 de wrangler dev) — `cog_url` resuelve. Fases siguientes en `docs/plan-implementacion.md`.

**F2 (mapa + raster) hecho.** Catálogo rico de productos raster en `shared/products/` (módulo TS por `product_code`, paletas **draft estilo NWS — validación del experto pendiente**, puerta M2), tabla nivel→color 256 entradas (0=nodata, 1=range folded, recorte por `max_level`) en `shared/products/palette.ts`, proyección AEQD dinámica + estilo WebGL en `utils/map/`, `MapLegend.vue`. Goldens visuales offline: COGs golden commiteados en `tests/fixtures/cogs/r2/<r2_key>` (espejo de las filas grabadas más recientes — **solo el vol_time más reciente de cada (site,product)**, el resto 404 offline por diseño), servidos por `scripts/serve-cogs.mjs` (CORS+Range) con `NUXT_PUBLIC_R2_BASE_URL` local; proyecto Playwright `goldens` aparte y en serie (contextos WebGL concurrentes en SwiftShader pierden contexto). Imports cliente de `shared/` SIEMPRE vía alias `#shared` (el path relativo rompe el bundle nitro). Falta para M2: validación QGIS + paletas confirmadas por el experto. Los 7 productos raster renderizan sin error (verificado runtime).

**F3 (selección + timeline + animación + URL) hecho.** Decisión clave que amplía el stack fijado: **todo el estado de la UI se maneja con XState v5** (`xstate` + `@xstate/vue`), URL como fuente de verdad (decisión 18, `docs/decisiones.md`) — máquinas en `machines/`, diagramas y notas de implementación vivas en `docs/maquinas-estado.md` (actualizar siempre en el mismo commit que toca una máquina). Página viewer migrada de `pages/index.vue` a `pages/[site]/[product]/[[time]].vue` (datetime compacto `YYYYMMDDTHHMMSS` en el path, `shared/url/time-path.ts`); `pages/index.vue` queda como redirect client-side a la última selección (`lamula:prefs` en localStorage, nunca el time). `viewerMachine` (`machines/viewer.ts`) es `type:'parallel'` con regiones `raster` (frame mostrado, stepping local + `/api/rasters/{next,prev}` en extremos) y `timeline` (rasters del día vía `/api/rasters/day`, día picker de 72h anclado a `last_seen_at`, no wall-clock). **Nota XState v5 importante**: en una máquina paralela, un `on` de la raíz queda ensombrecido en cuanto una región define el suyo para el mismo evento — no burbujea (confirmado con test canario antes de asumirlo). Animación: pool de `WebGLTileLayer` (una por frame, comparten un contexto WebGL, prefetch acotado a 3 concurrentes, "frame listo" = `renderComplete` del renderer muestreado en `postrender` — propiedad semi-pública protegida por `tests/unit/render-complete-canary.spec.ts`) en `utils/map/frame-pool.ts`, orquestado por `animationMachine`+`frameMachine` (`machines/animation.ts`, `machines/frame.ts`); `RadarMap.vue` tiene modo dual retrocompatible (`:raster` estático de F2 intacto — de eso dependen los goldens; `:frames` activa el pool). **Hallazgo recurrente de e2e** (no solo unit): esta app SSR con `<script setup>` async sufre una carrera de hidratación real — el evento nativo (`change`/`click`) puede llegar antes de que Vue adjunte su listener justo tras `page.goto()`; para un `<select>` (acción idempotente) el fix es reintentar con Playwright `toPass`, pero para un botón toggle (acción con efecto, ej. play/pause) reintentar el click es INCORRECTO (puede pisar un auto-play) — ahí el fix es esperar `page.waitForLoadState('networkidle')` antes del único click. Faltó por hacer explícitamente en el plan original y no bloquea M3: overlays de fenómenos/VWP (F4).

**F4 (fenómenos + VWP) hecho — puerta M4 pendiente de validación del experto con datos vivos.** Alcance recortado acordado con el pipeline (D22): sin VIL/top/granizo por celda (SS/HI no fluyen en el feed) — tabla ordena por `dbz_max` (puede faltar: el GAB de NST pagina de a 6), tendencia = dBZ máx + altura, TVS = flag del NMD. Tipos+parsers tolerantes de `attrs` por kind en `shared/contract/attrs.ts` (toda clave opcional, un campo corrupto no tumba la fila). **Join temporal (D24)**: fenómenos/VWP tienen vol_times propios — índices `GET /api/{phenomena,vwp}/times?site&day` + `nearestWithin(times, t, 600s)` en cliente (`utils/overlay/join.ts`), cache inmutable por vol_time; fuera de tolerancia el overlay se limpia, nunca celdas viejas como actuales. Estado en URL (D23): `?layers=cells,meso&panel=cells|trend|vwp&cell=ID`, defaults off (URLs F3 intactas), eventos raíz `TOGGLE_LAYER/SELECT_PANEL/SELECT_CELL` + `syncOverlayQuery` (replace inmediato, sin debounce). `overlayMachine` separada (D27, patrón animationMachine, `machines/overlay.ts`): regiones index/frame/series/vwp, **ningún evento en la raíz** — en la difusión a regiones paralelas los guards ven el snapshot previo (usar payload del evento) pero las acciones corren en orden de documento sobre contexto ya actualizado. Capa vectorial `utils/map/phenomena-layer.ts` (estilos cacheados por clave, declutter; tracks `[x_km,y_km]` AEQD ×1000 + transform). **Semántica de tracks deducida de grabaciones** (D25, confirmar con experto): `past` reciente→viejo, `movement_deg` "desde" — canario `tests/unit/tracks.spec.ts`; `meso.cell_id` es el ID del mesociclón, la celda va en `attrs.storm_id`. Matemática reimplementada (legado no disponible): barbas WMO/u-v/escalas en `utils/wind/`, `utils/charts/`; charts y VWP en SVG propio. Panel derecho colapsable con rail (D26) — el rail estrecha el mapa 36px: **los baselines golden se regeneraron enteros por eso** (mismo render). Caso e2e completo: BYX 03:08:18 (único vol grabado con raster+celdas+meso+VWP; COGs golden BYX bajados ex profeso antes de la purga de retención de 72h — `mesoVolume` en derive.ts exige volumen con meso Y raster). i18n sigue siendo F5 (strings ES hardcodeados); PrimeVue sigue sin uso (tabla nativa).

El plan reconciliado vive en `docs/` (MkDocs Material, español), desplegado a Cloudflare Pages (proyecto `lamula-webviewer-docs`) al tocar `docs/**` o `mkdocs.yml` en `main`.

## Qué es

Viewer web de productos NEXRAD Level III: reescritura de VestaWeb2 (Svelte 3 + Django) como **Nuxt 3** sobre **Cloudflare Pages**. Lee (solo lectura) los almacenes de **nexrad-l3-pipeline**: COGs AEQD calibrados en R2 (render cliente con OpenLayers `WebGLTileLayer` + `ol/source/GeoTIFF`, paleta como color ramp) y D1 (radares/rasters/fenómenos/VWP, vía binding en server routes).

## Reglas clave (ver docs/decisiones.md — no re-litigar sin motivo)

- El contrato de datos lo posee nexrad-l3-pipeline (`db/migrations/` de aquel repo). Este proyecto **jamás escribe** en D1 ni aplica migraciones; solo `SELECT`.
- Paletas y catálogo rico de productos viven **en este repo** (módulos TS por `product_code`), no en la base.
- Radar-agnóstico: `radars.proj4` se registra tal cual; nada hardcodeado.
- DAL con adaptadores live (D1/R2) y fixture (grabaciones + COGs golden) — todo lo de arriba es agnóstico a la fuente.
- Stack fijado: Nuxt 3 (preset `cloudflare-pages`), **XState v5 + `@xstate/vue`** (decisión 18 — todo el estado de UI, URL como fuente de verdad; ver `docs/maquinas-estado.md`), Tailwind, PrimeVue v4 unstyled, `@nuxtjs/i18n` (es+en), OpenLayers ≥ 9 + proj4, Vitest + Playwright. Pinia sigue instalado (módulo Nuxt cargado) pero sin uso — el estado de UI vive en XState.
- Calibración raster: `físico = nivel · value_scale + value_offset` (niveles ≥ 2); 0 = nodata, 1 = range folded.

## Comandos

```bash
pnpm dev / lint / typecheck / test / build         # app (ver README)
pnpm preview                                       # wrangler pages dev sobre dist/
pnpm test:e2e                                      # Playwright (requiere pnpm build previo)
uvx --with mkdocs-material mkdocs serve            # preview docs en :8000
uvx --with mkdocs-material mkdocs build --strict   # lo que corre CI de docs
```
